import 'package:flutter/material.dart';

class AppConstants {
  static const Color primaryColor = Color(0xFF2196F3);
  static const Color activeNodeColor = Color(0xFF4CAF50);
  static const Color nodeColor = Color(0xFF9E9E9E);
  static const Color connectionColor = Color(0xFF757575);
  
  static const double nodeSize = 50.0;
  static const double activeNodeSize = 60.0;
  static const double connectionStroke = 2.0;
  
  static const Duration animationDuration = Duration(milliseconds: 300);
}